// const Button = {};
// [playBtn, hintBtn, river, redBtn, purpleBtn, normalModeBtn, arcadeModeBtn, modeBtn]

const locations = ["Austin", "New York", "San Francisco"];
locations.forEach(location => {
    document.getElementById('playBtn')
});